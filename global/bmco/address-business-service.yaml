APP_API_NAME: address-business-service
AppVersion: 1.14.0 

replicaCount: 3

image:
  repository: mongodb
  pullPolicy: IfNotPresent

configmap_properties:
    name: name from global
    age:  age from global 
    bird: peacock from global 
 

