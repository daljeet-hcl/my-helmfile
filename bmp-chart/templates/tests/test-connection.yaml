apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "addressBusinessService.fullname" . }}-test-connection"
  labels:
    {{- include "addressBusinessService.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "addressBusinessService.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
