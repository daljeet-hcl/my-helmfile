APP_API_NAME: address-business-service-prod
AppVersion: 1.15.0 

image:
  repository: nginx
  pullPolicy: IfNotPresent
  
 
configmap_properties:
  name: "name from global"
  age:  age from global
  address: address from global 

