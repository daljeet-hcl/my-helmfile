APP_API_NAME: service-registry-business-service-dev1
AppVersion: latest

image:
  repository: tomcat
  pullPolicy: IfNotPresent

configmap_properties: 
    name: "nameFrom dev1"
    age:  "age from dev1" 